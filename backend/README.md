# Backend

Backend service placeholder for MOVZZ.

## Suggested next structure
- `src/` for API and service modules
- `tests/` for integration and unit tests
- `prisma/` or `migrations/` for database schema evolution

## Suggested stack (from your blueprint)
- Node.js + TypeScript
- PostgreSQL
- Redis/BullMQ
